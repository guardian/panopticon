/**
  * React frontend client build commands.
  */
object ClientCommands {
  val dependencyInstall: String = "yarn install"
  val test: String = "yarn run test"
  val serve: String = "yarn run start"
  val build: String = "yarn run build"
}
